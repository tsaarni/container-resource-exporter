apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: container-resource-exporter
  labels:
    app: container-resource-exporter
spec:
  selector:
    matchLabels:
      app: container-resource-exporter
  template:
    metadata:
      labels:
        app: container-resource-exporter
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: exporter
          image: ghcr.io/tsaarni/container-resource-exporter:latest
          imagePullPolicy: IfNotPresent
          command:
            - /container-resource-exporter
          args:
            - -config=/config/config.yaml
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /config/config.yaml
              subPath: config.yaml
              readOnly: true
            # Host access required for reading container metrics
            - name: cgroup
              mountPath: /sys/fs/cgroup
              readOnly: true
            - name: proc
              mountPath: /proc
              readOnly: true
            # Host socket access required for container runtime communication
            - name: containerd
              mountPath: /run/containerd/containerd.sock
          securityContext:
            # Must run as root to access host volumes (cgroup, proc, containerd socket)
            runAsUser: 0
            privileged: true
      volumes:
        - name: config
          configMap:
            name: container-resource-exporter-config
        - name: cgroup
          hostPath:
            path: /sys/fs/cgroup
            type: Directory
        - name: proc
          hostPath:
            path: /proc
            type: Directory
        - name: containerd
          hostPath:
            path: /run/containerd/containerd.sock
            type: Socket
      # Run on all nodes including master
      #tolerations:
      #  - effect: NoSchedule
      #    operator: Exists
---
apiVersion: v1
kind: Service
metadata:
  name: container-resource-exporter
  labels:
    app: container-resource-exporter
spec:
  type: ClusterIP
  clusterIP: None  # Headless service for DaemonSet
  ports:
    - port: 8080
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: container-resource-exporter
